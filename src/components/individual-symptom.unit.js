import IndividualSymptom from './individual-symptom'

describe('@components/individual-symptom', () => {
  it('exports a valid component', () => {
    expect(IndividualSymptom).toBeAComponent()
  })
})
